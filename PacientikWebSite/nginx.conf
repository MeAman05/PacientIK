server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    include /etc/nginx/mime.types;
    types { application/wasm wasm; }
    location /_framework/ { try_files $uri =404; }
    location / { try_files $uri $uri/ /index.html =404; }
}
